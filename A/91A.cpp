#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
main()
{
    
    string a,b;
    cin>>a>>b;
    for(int i =0; i < a.size();i++)
    {
        for(int j =0; j < b.size();i++)
        {
            if(a.substr(i,j) == )

        }
    }
 
}